#include <stdint.h>
#include <vector>

#define vec std::vector
typedef int32_t i32;
typedef std::vector<char> *str;

#define add(a, b) ((a) + (b))
#define sub(a, b) ((a) - (b))
#define mul(a, b) ((a) * (b))
#define div(a, b) ((a) / (b))
#define mod(a, b) ((a) % (b))
#define less(a, b) ((a) < (b))
#define greater(a, b) ((a) > (b))
#define less_eq(a, b) ((a) <= (b))
#define greater_eq(a, b) ((a) >= (b))
#define eq(a, b) ((a) == (b))
#define neq(a, b) ((a) != (b))
