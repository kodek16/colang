fun main() {
  var x = 3;
  var y = 5;
  assert(x < y);
  assert(x <= y);
  assert(y > x);
  assert(y >= x);
  assert(x != y);

  var z = 5;
  assert(y <= z);
  assert(y >= z);
  assert(z <= y);
  assert(z >= y);
  assert(y == z);
}
